{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run active C++ (exe files)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}\\exe files\\${fileBasenameNoExtension}.exe",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "Build active C++ to exe files",
      "MIMode": "gdb",
      "externalConsole": true
    }
  ]
}
